---

- name: deploy attendence app files to remote
  copy:
    src: attendance
    dest: /usr/local/
    remote_src: no
- name: Change ownership of a directory
  file:
    path: /usr/local/attendance
    state: directory
    recurse: yes
    mode: '0777'
